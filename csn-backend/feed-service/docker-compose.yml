services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: csn
      POSTGRES_PASSWORD: csn_pass
      POSTGRES_DB: csn_feed
    ports:
      - "5441:5432"
    volumes:
      - feed_pgdata:/var/lib/postgresql/data

volumes:
  feed_pgdata:
