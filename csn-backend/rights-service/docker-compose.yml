services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: csn
      POSTGRES_PASSWORD: csn_pass
      POSTGRES_DB: csn_rights
    ports:
      - "5442:5432"
    volumes:
      - rights_pgdata:/var/lib/postgresql/data

volumes:
  rights_pgdata:
