services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: csn
      POSTGRES_PASSWORD: csn_pass
      POSTGRES_DB: csn_voice
    ports:
      - "5436:5432"
    volumes:
      - voice_pgdata:/var/lib/postgresql/data

volumes:
  voice_pgdata:
