services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: csn
      POSTGRES_PASSWORD: csn_pass
      POSTGRES_DB: csn_voting
    ports:
      - "5440:5432"
    volumes:
      - voting_pgdata:/var/lib/postgresql/data

volumes:
  voting_pgdata:
