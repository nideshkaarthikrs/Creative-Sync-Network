version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: csn
      POSTGRES_PASSWORD: csn_pass
      POSTGRES_DB: csn_identity
    ports:
      - "5432:5432"
    volumes:
      - identity_pgdata:/var/lib/postgresql/data

volumes:
  identity_pgdata:
