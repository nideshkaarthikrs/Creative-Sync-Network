services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: csn
      POSTGRES_PASSWORD: csn_pass
      POSTGRES_DB: csn_project
    ports:
      - "5438:5432"
    volumes:
      - project_pgdata:/var/lib/postgresql/data

volumes:
  project_pgdata:
