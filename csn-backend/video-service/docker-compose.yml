services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: csn
      POSTGRES_PASSWORD: csn_pass
      POSTGRES_DB: csn_video
    ports:
      - "5437:5432"
    volumes:
      - video_pgdata:/var/lib/postgresql/data

volumes:
  video_pgdata:
